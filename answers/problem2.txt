//Tests
public final class GraphWrapperTest{
	
	//Edge Class Testing
	@Test
	public void testEdgeConstructor()
	{
		new Edge("Aasdf", "aasdf", "asdfsdaf" );
		new Edge("asdf", "aasdf", "asdfsdaf" );
		String a  = new String("asdfasafw");
		new Edge(a, a, a);
	}
	
	@Test
	public void testgetChild()
	{
		Edge e = new Edge("Aasdf", "aasdf", "asdfsdaf" );
		assertEquals(e.getChild(), "asdfsdaf");
		String a  = new String("asdfasafw");
		e = new Edge(a, a, a);
		assertEquals(e.getChild(),a);
	}
	
	@Test
	public void testgetParent()
	{
		Edge e = new Edge("Aasdf", "aasdf", "asdfsdaf" );
		assertEquals(e.getParent(),"aasdf");
		String a  = new String("asdfasafw");
		e = new Edge(a, a, a);
		assertEquals(e.getParent(), "asdfasafw");
	}
	
	@Test
	public void testgetLabel()
	{
		Edge e = new Edge("Aasdf", "aasdf", "asdfsdaf" );
		assertEquals(e.getLabel(),"Aasdf");
		String a  = new String("asdfasafw");
		e = new Edge(a, a, a);
		assertEquals(e.getLabel(), a);
	}
	
	@Test
	public void testequals()
	{
		Edge e = new Edge("sadf", "asdf", "asdf");
		Edge a = new Edge("sadf", "asdf", "asdf");
		Edge b = new Edge("asdf", "asdfss", "asdf");
		Edge c = new Edge("sadf", "asdfd", "asdf");
		Edge d = new Edge("sadf", "asdf", "234ds");
		
		assertEquals(e.equals(a), true);
		assertEquals(e.equals(b), false);
		assertEquals(e.equals(c), false);
		assertEquals(e.equals(d), false);
	}
	
	
	
	//Node Class Testing
	@Test
	public void testNodeConstructor() {
		new Node("sdf");
		String s = new String("asdfsadf");
		new Node(s);
	}
	
	@Test
	public void testAddEdgeOneArg()
	{
		Edge e = new Edge("asdf", "asdf", "sdfdsa");
		Node n = new Node("people");
		n.addEdge(e);
		assertEquals(n.NumOfEdges(), 1);
		ArrayList<Edge> list = n.getEdges();
		assertEquals(list.get(0).equals(e), true);
		
		e = new Edge("asdp", "asasdfdf", "sdfdasdfsa");
		n.addEdge(e);
		assertEquals(n.NumOfEdges(), 2);
		list = n.getEdges();
		assertEquals(list.get(1).equals(e), true);	
		
		e = new Edge("asdasdfp", "asasdfdf", "asasdfdf");
		n.addEdge(e);
		assertEquals(n.NumOfEdges(), 3);
	}
	
	@Test
	public void testaddEdgeMultArgs() {
		Edge e = new Edge("asdf", "asdf", "sdfdsa");
		Node n = new Node("sdf");
		n.addEdge("asdf", "asdf", "sdfdsa");
		assertEquals(n.NumOfEdges(), 1);
		ArrayList<Edge> list = n.getEdges();
		assertEquals(list.get(0).equals(e), true);
		
		e = new Edge("asdp", "asasdfdf", "sdfdasdfsa");
		n.addEdge("asdp", "asasdfdf", "sdfdasdfsa");
		assertEquals(n.NumOfEdges(), 2);
		list = n.getEdges();
		assertEquals(list.get(1).equals(e), true);
	}
	
	@Test
	public void testgetName() {
		Node a = new Node("sdf");
		assertEquals(a.getName().equals("sdf"), true);
		String s = new String("asdfsadf");
		Node b = new Node(s);
		assertEquals(b.getName().equals(s), true);
	}
	
	@Test
	public void testgetEdges() {
		Node a = new Node("sdf");
		ArrayList<Edge> list = a.getEdges();
		assertEquals(list.size(), 0);
		
		Edge e = new Edge("asdp", "sdf", "sdfdasdfsa"); 
		a.addEdge(e);
		list = a.getEdges();
		assertEquals(list.size(), 1);
		assertEquals(list.get(0).equals(e), true);
		
		Edge b = new Edge("aset", "sdf", "sdfdasdfsa4"); 
		a.addEdge(b);
		list = a.getEdges();
		assertEquals(list.size(), 2);
		assertEquals(list.get(1).equals(b), true);
	}
	
	
	
	//Graph Tests
	@Test
	public void testGraphConstructor()
	{
		new Graph();
	}
	
	@Test
	public void testGraphgetNumOfEdges()
	{
		Graph g = new Graph();
		assertEquals(g.getNumOfEdges(), 0);
		Node n = new Node("asdf");
		g.addNode(n);
		n = new Node("bcdf");
		g.addNode(n);
		ArrayList<Node> list = g.getNodes();
		Edge e = new Edge("23", list.get(0).getName(), list.get(1).getName());
		g.addEdge(e);
		assertEquals(g.getNumOfEdges(), 1);
	}
	
	@Test
	public void testGraphNumOfNodes()
	{
		Graph g = new Graph();
		assertEquals(g.getNumOfNodes(), 0);
		Node n = new Node("asdf");
		g.addNode(n);
		assertEquals(g.getNumOfNodes(), 1);
	}
	
	@Test
	public void testGraphAddNodeNodeArg()
	{
		Graph g = new Graph();
		Node n = new Node("node");
		g.addNode(n);
		assertEquals(g.getNumOfNodes(), 1);
		g.addNode(n);
		assertEquals(g.getNumOfNodes(), 1);
	}
	
	@Test
	public void testGraphAddNodeStringArg()
	{
		Graph g = new Graph();
		g.addNode("2cat");
		assertEquals(g.getNumOfNodes(), 1);
		g.addNode("2cat");
		assertEquals(g.getNumOfNodes(), 1);
	}
	
	@Test
	public void testGraphAddEdgeOneArg()
	{
		Graph g = new Graph();
		assertEquals(g.getNumOfEdges(), 0);
		Node n = new Node("asdf");
		g.addNode(n);
		n = new Node("bcdf");
		g.addNode(n);
		ArrayList<Node> list = g.getNodes();
		Edge e = new Edge("23", list.get(0).getName(), list.get(1).getName());
		g.addEdge(e);
		assertEquals(g.getNumOfEdges(), 1);
		g.addEdge(new Edge("23", list.get(0).getName(), "asdafadf"));
		assertEquals(g.getNumOfEdges(), 1);
		g.addEdge(new Edge("2sdf", "asdfasdasdf", "asdafadf"));
		assertEquals(g.getNumOfEdges(), 1);
		g.addEdge(e);
		assertEquals(g.getNumOfEdges(), 1);
	}
	
	@Test
	public void testGraphAddEdgeMultArgs()
	{
		Graph g = new Graph();
		assertEquals(g.getNumOfEdges(), 0);
		Node n = new Node("asdf");
		g.addNode(n);
		n = new Node("bcdf");
		g.addNode(n);
		ArrayList<Node> list = g.getNodes();
		g.addEdge("23", list.get(0).getName(), list.get(1).getName());
		assertEquals(g.getNumOfEdges(), 1);
		g.addEdge("23", list.get(0).getName(), "asdafadf");
		assertEquals(g.getNumOfEdges(), 1);
		g.addEdge("2sdf", "asdfasdasdf", "asdafadf");
		assertEquals(g.getNumOfEdges(), 1);
		g.addEdge("23", list.get(0).getName(), list.get(1).getName());
		assertEquals(g.getNumOfEdges(), 1);
	}
	
	@Test
	public void testGraphGetNodes()
	{
		Graph g = new Graph();
		ArrayList<Node> list = g.getNodes();
		assertEquals(list.size(), 0);
		g.addNode("asdf");
		list = g.getNodes();
		assertEquals(list.size(), 1);
	}
	
	
	//GraphWrapper Tests
	@Test
	public void testGraphWrapperConstructor()
	{
		new GraphWrapper();
	}
	
	@Test
	public void testGraphWrapperaddNode()
	{
		GraphWrapper g = new GraphWrapper();
		g.addNode("asdf");
		g.addNode("asdfdsaf");
	}
	
	@Test
	public void testGraphWrapperaddEdge()
	{
		GraphWrapper g = new GraphWrapper();
		g.addNode("Rick");
		g.addNode("Morty");
		g.addEdge("Rick", "Morty", "Adventures");
	}
	
	@Test
	public void testGraphWrapperlistNodes()
	{
		GraphWrapper g = new GraphWrapper();
		g.addNode("Rick");
		g.addNode("Morty");
		g.addNode("Bob");
		g.addEdge("Rick", "Morty", "Adventures");
		g.addEdge("Rick", "Rick", "Adventures");
		g.addEdge("Morty", "Morty", "Adventures");
		
		Iterator<String> nodeNames = g.listNodes();
		String a = nodeNames.next();
		while(nodeNames.hasNext())
		{
			String b = nodeNames.next();
			assertEquals(a.compareTo(b) < 0, true);
			a = b;
		}
	}
	
	@Test
	public void testGraphWrapperlistChildren()
	{
		GraphWrapper g = new GraphWrapper();
		g.addNode("Rick");
		g.addNode("Morty");
		g.addEdge("Rick", "Morty", "Adventures");
		g.addEdge("Rick", "Rick", "Science");
		g.addEdge("Morty", "Morty", "dbf");
		
		Iterator<String> childNames = g.listChildren("Rick");
		String a = childNames.next();
		while(childNames.hasNext())
		{
			String b = childNames.next();
			assertEquals(a.compareTo(b) < 0, true);
			a = b;
			
		}
	}
}

//Documentation
My Strategy for this hw was to create a test cases for every method. Before I wrote the test cases for each method I thought of what test
cases are simple and easy to do. I wrote them down and then I started thinking of harder test cases based upon the spec that I wrote for 
each method. I thought of where the method could go wrong and different ways how others might use these methods. Each method had at least 
one test case. No method was left without a test case. I did the simple test cases for methods first.


I feel my tests are sufficient because the cover a lot of ground and I have done as much as possible. I did not get an urge to add more tests
since I built off my test and I don't think other tests will be possible. 